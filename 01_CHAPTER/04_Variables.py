# In Python, variables are used to store data that can be Used and manipulated throughout a program. A variable is created the moment you assign a value to it using the assignment operator ( = ). 


age = 34 # integer
name = "Dhruba" # string
cgpa = 4.55 # float

# Rule of defining a variable in Python 
#   1.Variable name must start with a letter ( a-z , A-Z ) or an undercore(_).
#   2.They can contain letters , numbers , and underscores .
#   3.Variable names are case-sensitive (age and Age are different)
#   4.Avoid using Python keywords (e.g., if , for , while ) as variable names


# 34age = 4  # Invalid because variable name start with a number 
age = 32     # Valid because variable name doesn't start with a number

# a$$ge = 45 # Invlaid because variables cannot contain special characters other than _
__age = 34
__nice_45 = 34
a_b_c_7 = "Sam"