# Reverse the string text using slicing.

text = "Hello World"
reversed_text = text[::-1]
print(reversed_text)
# The syntax text[start:stop:step] is used for slicing.
# [::-1] means:
# start from the end (:),
# go till the beginning (:),
# step backwards by 1 (-1).