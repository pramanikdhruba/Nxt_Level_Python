# Create a string variable name with your full name. Print:
# The first character
# The last character
# The length of the string

full_name = "Dhruba Pramanik"
first_char = full_name[0]
last_char = full_name[-1]
lenght_name = len(full_name)
print(full_name,first_char,last_char,lenght_name,sep=", ")