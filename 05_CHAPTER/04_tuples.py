# WHAT IS TUPLES ?
#   In Python, a tuple is a collection (like a list), but with some key differences.

#🔹 Definition: 
#       A tuple is an ordered, immutable collection of items.
#          1.Ordered → items have a fixed order.
#          2.Immutable → once created, you cannot change it (no adding, removing, or modifying elements).
#          3.Written with parentheses ( ).

a = (3, 2, 22, 13)

print(a)
print(a[2])
a[3] = 32

b = (3, )